import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.jobjingjo.mobsf',
  appName: 'test-mobsf',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
